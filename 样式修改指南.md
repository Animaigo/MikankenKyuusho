# 样式修改指南（少女终末旅行风格）

> 给没有编程基础的你：改风格 = 改几个文件里的数字/颜色，保存后浏览器刷新就能看到效果。
> 修改前建议先备份文件，或只改一处、刷新看一处。

## 改哪里会变什么

| 想改的东西 | 改哪个文件 | 说明 |
| --- | --- | --- |
| 整体配色（最重要） | `src/styles/colors.scss` | 浅色/深色两套颜色都在这里，改这一处气质变化最大 |
| 页面底色渐变 | `src/styles/global.scss` | 开头 `body` 里的 `linear-gradient(...)`，现在是粉色 |
| 背景大图 | `public/bg.avif`、`public/bg-dark.avif` | 直接拿新图覆盖同名文件即可（推荐 16:9 横图） |
| 字体 | `src/components/BaseHead.astro` + `global.scss` | 字体引入在 BaseHead，应用在 `global.scss` 的 `font-family` |
| 卡片/正文/标签样式 | `src/styles/components.scss`、`article.scss` | 布局和组件细节 |
| 某个组件单独样式 | 各组件自带的 `<style>` | 如 `Card.astro`、`Header.astro` |

## 少女终末旅行参考配色（可直接抄）

这套颜色偏"末世后的小温暖"：米白、军绿、暗锈橙、炭灰，没有高饱和。

### 浅色模式（`:root` 里）
```scss
--color-primary: #6e6a45;        // 主色：军绿
--color-background: #ece8dd;      // 页面背景：米白
--color-foreground: #2e2d28;      // 正文文字：墨炭灰
--color-muted-foreground: #6e6a5e;// 次要文字
--color-card: #f5f2e9;            // 卡片底色
--color-highlight: #a8784f;       // 高亮：暗锈橙
```

### 深色模式（`html.dark` 里）
```scss
--color-primary: #a3a066;         // 主色：苔藓绿
--color-background: #14140f;      // 背景：炭黑
--color-foreground: #e8e4d8;      // 文字：暖白
--color-muted-foreground: #9c988a;
--color-card: #1f1e18;
--color-highlight: #c09a6a;
```

### 页面底色渐变（global.scss）
把粉色的两行换成：
```scss
background-image: linear-gradient(to right, #e6e1d3, #d8d3c2);   // 浅色
// dark 模式：
background-image: linear-gradient(to right, #14140f, #1c1b15);   // 深色
```

## 常见流程

1. 用 VS Code 打开项目，改 `colors.scss`（边改边看 `http://localhost:4321/MikankenKyuusho/`）。
2. 感觉颜色差不多了，再换背景图：把图丢进 `public/`，文件名覆盖 `bg.avif` 和 `bg-dark.avif`。
3. 最后微调字体/卡片圆角等细节。
4. 都满意后，`pnpm build` 构建成功再推 GitHub。

## 小提示
- `bg.avif` 是浅色模式背景，`bg-dark.avif` 是深色模式背景，各准备一张。
- 图片体积尽量小（<500KB），否则页面加载慢。
- 想完全换一种感觉，先只动 `colors.scss`，通常就够了。