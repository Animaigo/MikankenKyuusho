# 部署指南（GitHub Pages）

> 目标：把本地的新主题博客发布到 https://animaigo.github.io/MikankenKyuusho/
> git 仓库、远程、身份已提前配好，你只需按下面 1、2 两步操作。

## 1. 本地操作（在项目文件夹打开终端）

项目文件夹：`C:\Users\luosh\Documents\ChatGPT\Codx project\MikankenKyuusho`

```powershell
# ① 构建验证：必须成功（跑类型检查 + 构建，通过后会出现 dist/ 文件夹）
pnpm build

# ②（只第一次需要）信任这个 git 仓库，否则会报 dubious ownership
git config --global --add safe.directory 'C:/Users/luosh/Documents/ChatGPT/Codx project/MikankenKyuusho'

# ② 构建通过后，提交并推送（第一次用 --force，会覆盖仓库里的旧版内容）
git add -A
git commit -m "用 Koibumi 主题重建博客"
git push -u origin main --force
```

> 提示：push 时会弹出 GitHub 登录窗口，正常登录即可。
> 如果 `pnpm build` 报错，把错误发给我，我修好你再重新构建、推送。

## 2. GitHub 网页设置（只需一次）

1. 打开 https://github.com/Animaigo/MikankenKyuusho
2. **Settings → General** → 页面最下方 Danger Zone → 确认仓库是 **Public**（免费版 Pages 要求公开仓库）
3. **Settings → Pages** → Source 选择 **GitHub Actions**
4. 回到仓库 **Actions** 标签页，等部署跑完（绿色对勾）
5. 访问 https://animaigo.github.io/MikankenKyuusho/ 验证

## 3. 以后每次更新

```powershell
pnpm build          # 先确认能构建成功
git add -A
git commit -m "更新内容"
git push            # 以后不要加 --force
```

等 Actions 跑完，网站自动更新。